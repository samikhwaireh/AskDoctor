<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.askdoctors">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Activity.ChatActivity" />
        <activity android:name=".Activities.Activity.UpdateProfileActivity" />
        <activity android:name=".Activities.Activity.ProfileActivity" />
        <activity android:name=".Activities.Activity.AnswerActivity" />
        <activity android:name=".Activities.Activity.ShowDoctorsProof" />
        <activity android:name=".Activities.Activity.AskQuestionActivity" />
        <activity android:name=".Activities.Activity.SetProfileInfo_Activity" />
        <activity android:name=".Activities.Activity.AdminActivity" />
        <activity android:name=".Activities.Activity.DoctorsProof" />
        <activity android:name=".Activities.Activity.HomeActivity" />
        <activity android:name=".Activities.Activity.SignUpActivity" />
        <activity android:name=".Activities.Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>